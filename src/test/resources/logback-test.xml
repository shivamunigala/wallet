<?xml version="1.0" encoding="UTF-8"?>
<!--
  Tests run at INFO with the noisy infrastructure loggers turned down. Testcontainers and
  its shaded HTTP client log entire Docker API payloads at DEBUG, which buries the
  assertion failures this suite exists to surface.
-->
<configuration>
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} %-5level [%X{correlation_id:-}] %logger{28} - %msg%n</pattern>
        </encoder>
    </appender>

    <logger name="com.github.dockerjava" level="WARN"/>
    <logger name="org.testcontainers" level="INFO"/>
    <logger name="org.springframework" level="WARN"/>
    <logger name="org.hibernate" level="WARN"/>
    <logger name="com.zaxxer.hikari" level="WARN"/>
    <logger name="com.shiva.wallet" level="INFO"/>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>
</configuration>
